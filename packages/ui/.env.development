# Local dev: leave empty to use Vite proxy (requests go to localhost:8788, avoids CORS)
# Production: set to https://backend.appfounder.ca when UI at cp.appfounder.ca
# Leave empty so API calls use relative paths (/api, /auth) → Vite proxy forwards to localhost:8788
VITE_SERVER_URL=
VITE_PROGRAM_ID=WRy4hstBsD6hxb7CJN4R3fgLnafs621N7EjUhZ2afze
